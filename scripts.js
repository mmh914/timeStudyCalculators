(()=>{'use strict';const D=['Monday','Tuesday','Wednesday','Thursday','Friday'],now=()=>new Date(),monday=d=>{let x=new Date(d),n=x.getDay();x.setDate(x.getDate()-(n===0?6:n-1));x.setHours(0,0,0,0);return x},ws=monday(now()),key=`fieldWeek:${ws.getFullYear()}-${ws.getMonth()+1}-${ws.getDate()}`,base={minimum:7,times:Object.fromEntries(D.map(d=>[d,{start:'',end:''}]))};let s;try{let v=JSON.parse(localStorage.getItem(key));s={minimum:Number(v?.minimum)||7,times:Object.fromEntries(D.map(d=>[d,{start:v?.times?.[d]?.start||'',end:v?.times?.[d]?.end||''}]))}}catch{s=base}const $=q=>document.querySelector(q),idx=()=>{let d=now().getDay();return d>0&&d<6?d-1:-1},date=i=>{let d=new Date(ws);d.setDate(d.getDate()+i);return d},mins=v=>{if(!v)return null;let[a,b]=v.split(':').map(Number);return a*60+b},elapsed=(e,live)=>{let a=mins(e.start),b=mins(e.end);if(a===null)return 0;if(b===null&&live){let n=now();b=n.getHours()*60+n.getMinutes()+n.getSeconds()/60}if(b===null)return 0;if(b<a)b+=1440;return Math.max(0,b-a)},dur=n=>`${Math.floor(Math.max(0,Math.round(n))/60)}h ${String(Math.max(0,Math.round(n))%60).padStart(2,'0')}m`,clock=n=>{n=((Math.round(n)%1440)+1440)%1440;return new Date(2000,0,1,Math.floor(n/60),n%60).toLocaleTimeString([],{hour:'numeric',minute:'2-digit'})},save=()=>{localStorage.setItem(key,JSON.stringify(s));$('#saved').textContent='✓ Saved just now';setTimeout(()=>$('#saved').textContent='✓ Saved on this device',1200)};function render(){let ti=idx();$('#rows').innerHTML=D.map((day,i)=>{let e=s.times[day],d=date(i),sameday=i===ti,t=elapsed(e,sameday);return`<div class="row ${sameday?'today':''}"><div class="day">${sameday?'<i></i>':''}${day}<small>${d.toLocaleDateString([],{month:'short',day:'numeric'})}${sameday?' · Today':''}</small></div><label><input aria-label="${day} first visit" type="time" data-day="${day}" data-field="start" value="${e.start}"></label><label><input aria-label="${day} last visit" type="time" data-day="${day}" data-field="end" value="${e.end}"></label><span class="total ${t?'':'empty'}">${t?dur(t):'—'}</span></div>`}).join('');let daily=s.minimum*60,weekly=daily*5,vals=D.map((d,i)=>elapsed(s.times[d],i===ti)),week=vals.reduce((a,b)=>a+b,0),today=ti>=0?vals[ti]:0;$('#todayTotal').textContent=dur(today);$('#weekTotal').textContent=dur(week);$('#weekNote').textContent=`of ${dur(weekly)} weekly target`;$('#progress').style.width=`${weekly?Math.min(100,week/weekly*100):100}%`;let e=ti>=0?s.times[D[ti]]:null;$('#todayNote').textContent=ti<0?'The workweek resumes Monday':!e.start?'Add today’s start time':!e.end?'Live since your first visit':'Today is complete';if(ti<0){$('#needed').textContent='—';$('#neededNote').textContent='Available Monday through Friday'}else if(!e.start){$('#needed').textContent='—';$('#neededNote').textContent='Enter today’s start time'}else{let prior=vals.slice(0,ti).reduce((a,b)=>a+b,0),through=daily*(ti+1),need=Math.max(0,through-prior);$('#needed').textContent=need===0?'Target met':clock(mins(e.start)+need);$('#neededNote').textContent=need===0?'You’re already caught up through today':`To reach ${dur(through)} by end of today`}let n=now();$('#clock').textContent=n.toLocaleString([],{weekday:'long',month:'short',day:'numeric',hour:'numeric',minute:'2-digit'})}let end=date(4);$('#weekRange').textContent=`${ws.toLocaleDateString([],{month:'long',day:'numeric'})}–${end.toLocaleDateString([],{month:'long',day:'numeric',year:'numeric'})}`;$('#minimum').value=s.minimum;$('#minimum').addEventListener('input',e=>{s.minimum=Math.max(0,Number(e.target.value)||0);save();render()});$('#rows').addEventListener('input',e=>{let x=e.target;if(!x.dataset.day)return;s.times[x.dataset.day][x.dataset.field]=x.value;save();render()});render();setInterval(render,30000)})();
